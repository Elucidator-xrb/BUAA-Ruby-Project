<div id="<%= dom_id comment %>">
  
  <p>
    <strong>
      Posted:
      <%= time_ago_in_words(comment.created_at) %> |
      Commented by:
      <%= comment.user.username %>
    </strong>
    <br/>
    <%= comment.content %>
  </p>

</div>
